# =============================================================================
# python_version/ 子目录专用 .gitignore
#
# 目标：推送到 GitHub 的只有「能直接 pip install 的源码 + 脚本 + 依赖清单」，
#      不上传任何运行时产物 / 大模型 / 本地环境 / 日志。
#
# 即便以后把这个目录独立拆分成单独仓库，这份文件也能直接工作。
# =============================================================================

# ---- 虚拟环境 / Python 缓存 ------------------------------------------------
.venv/
venv/
ENV/
env/

__pycache__/
*.py[cod]
*$py.class
.Python
*.egg-info/
pip-wheel-metadata/

# ---- 测试 / Lint 缓存 ------------------------------------------------------
.pytest_cache/
.ruff_cache/
.mypy_cache/
.hypothesis/
.coverage
.coverage.*
.cache
htmlcov/

# ---- IDE / 编辑器 ----------------------------------------------------------
.vscode/
.idea/
.trae/
.history/
*.swp
*.swo

# ---- 运行时产物（体积小，含隐私，不进仓）------------------------------------
*.log
*.out
debug.log
run.log
run_err.log

# ---- 本地配置（尺寸 / 热键 / 设备选择都是个性化的，首次启动自动生成）--------
config.json

# ---- 大模型 + OpenVINO 编译缓存（体积超大，几百 MB ~ 几 GB）-----------------
# 模型由 prepare_offline.py 在本地导出，或在首次翻译时生成编译缓存。
models/
model_cache/
